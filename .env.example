# -----------------------------------------------------------------
# Example Environment Variables for BYOD Synthetic Data Service
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named '.env' in the root directory.
# 2. The .env file is listed in .gitignore and will NOT be committed.
# 3. Fill in the values for your specific Azure deployment.
# For local development, you only need to fill in the services
# you are actively using (e.g., AZURE_OPENAI_...).
# -----------------------------------------------------------------

# -- Environment Configuration --
# CRITICAL: Set this correctly for each environment
# Options: local, development, production
ENVIRONMENT="local"

# -- CORS Security Settings --
# CRITICAL: Configure allowed origins to prevent security vulnerabilities
# Format: comma-separated list of allowed origins (no spaces after commas)
#
# Local development examples:
# Basic local:
ALLOWED_ORIGINS="http://localhost:8201,http://localhost:3000,http://127.0.0.1:8201"
#
# WSL + Windows VS Code + SSH scenarios (uncomment and modify as needed):
# ALLOWED_ORIGINS="http://localhost:8201,http://localhost:3000,http://127.0.0.1:8201,http://0.0.0.0:8201,http://localhost:8080"
#
# With specific VM IP for SSH port forwarding:
# ALLOWED_ORIGINS="http://localhost:8201,http://localhost:3000,http://192.168.1.100:8201,http://your-vm-hostname:8201"
#
# Development deployment example:
# ALLOWED_ORIGINS="https://byod-synthetic-data-service-dev.azurewebsites.net"
#
# Production deployment example (RESTRICTIVE):
# ALLOWED_ORIGINS="https://byod-synthetic-data-service.azurewebsites.net,https://yourdomain.com"

# -- Azure Function & App Service --
# Used for cloud deployment, can be ignored for local dev.
AZURE_TENANT_ID="your-azure-ad-tenant-id"
AZURE_CLIENT_ID="your-service-principal-client-id"
AZURE_CLIENT_SECRET="your-service-principal-client-secret"
WEBSITE_RUN_FROM_PACKAGE="1" # Or 0 for local dev

# -- Azure Storage Account --
# Can be ignored for local dev if not using blob storage locally.
AZURE_STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https..."
BLOB_STORAGE_CONTAINER_NAME="phi-uploads"

# -- Azure OpenAI Service --
# REQUIRED for local development.
AZURE_OPENAI_ENDPOINT="https://your-aoai-resource.openai.azure.com/"
AZURE_OPENAI_API_KEY="your-aoai-api-key"
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME="gpt-4" # The name of your chat model deployment
AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME="text-embedding-ada-002" # The name of your embedding model deployment
AZURE_OPENAI_API_VERSION="2024-02-01"

# -- Azure Cosmos DB (for Program Catalog) --
# Can point to a cloud instance or a local Docker container for dev.
COSMOS_DB_ENDPOINT="https://your-cosmos-account.documents.azure.com:443/"
COSMOS_DB_KEY="your-cosmos-db-primary-key"
COSMOS_DB_DATABASE_NAME="SyntheticData"
COSMOS_DB_CONTAINER_NAME="ProgramCatalog"

# -- Azure AI Search (for Vector Search) --
# Can be pointed to a cloud instance for dev.
AZURE_SEARCH_ENDPOINT="https://your-search-resource.search.windows.net"
AZURE_SEARCH_API_KEY="your-search-admin-api-key"
AZURE_SEARCH_INDEX_NAME="program-vectors"

# -- Application Settings --
GENERATOR_VERSION="1.0" # Used for versioning the cache
LOG_LEVEL="INFO" # e.g., DEBUG, INFO, WARNING, ERROR
APP_PORT="8201" # Default port for the application